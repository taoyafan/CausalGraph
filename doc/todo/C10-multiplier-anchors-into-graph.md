# C10 倍数假设 → g1/g2 锚点进图（Option A 全量拆解）

## 背景（当前状态）

各业务 `seg.*.rev_fy_mult`（全年/H1 收入倍数）是**直接拍定的 assumption 先验**（三角众数+带宽）。
其判断参考两个锚，二者**可由 audited 半年报精确算出，但目前仅作定性背景、尚未进图**：

- **g1**：同期内季节性增速 2025 H1→H2 = 2025H2 / 2025H1 − 1
- **g2**：最新跨年环比 2025H2→2026H1 = 2026H1 / 2025H2 − 1
- 恒等式：全年/H1 倍数 = (H1+H2)/H1 = 2 + g3，g3 = 2026 H1→H2 前瞻增速（唯一真正的假设）

各业务当前锚值（从 audited 报表推得，供进图核对，单位：亿元 / %）：

| 业务 | 2025H1 | 2025全年 | 2025H2 | 2026H1 | g1 | g2 | 现取 g3(众数) | 现倍数 |
|---|---|---|---|---|---|---|---|---|
| 电池化学品 | 28.15 | 66.79 | 38.64 | 57.506 | +37.3% | +48.8% | +40% | 2.40 |
| 电子信息化学品 | — | 14.65 | — | 9.563 | +15.8% | +21.7% | +24% | 2.24 |
| 有机氟化学品 | — | 14.26 | — | 7.083 | −2.5% | +0.6% | +2% | 2.02 |
| 其他业务 | — | 0.689 | — | 0.473 | — | — | ~0(粗取2.0) | 2.00 |

> 注：2025H1 分业务收入需从 `capchem_2025_semiannual_report.pdf` 提取（本机无 PDF 解析库，
> 表中除电池外的 2025H1 尚未逐一核出）。

## 目标（Option A）

1. `scout` 提取 audited：各业务 **2025H1 收入 + 2025 全年收入**（2026H1 已有）。
2. `operator-author` 补算子：`subtract`（2025H2 = 全年 − H1）、`环比`（a/b − 1，产出 g1/g2）、
   `affine`（2 + g3，产出倍数）。
3. 假设收缩为**只剩 `seg.*.g3` 一个前瞻先验**；`rev_fy_mult` 改由 `affine(2+g3)` 算子产出，
   g1/g2 变为图内可审锚点；quote 只保留 g3 的价格/产量判断一句。
4. `reviewer` 审核（含值来源零计算），跑 `python -m cgraph.cli check`。

## 为什么先不做

需要新的 audited 数据抽取 + 3 个新算子 + 重定 g3 先验（AI 判断，需协作者拍板）。
拆解前已先把 baked 计算移出 assumption 的 quote（改为直接先验 + 背景移入 source note），
数值不变，违规先消除；本条为后续的严谨化增量。
