# AGENTS.md — CausalGraph 协作约定

面向 AI Agent 与协作者的项目约定。开始工作前先读本文件。

## 协作行为约定

- **"记住" = 写进本文件**：只要协作者说"记住 X / 记一下 X"，就把 X 追加进
  本 `AGENTS.md`（而不是别处的 memory）。
- **每次提交同步 push**：每次 `git commit` 后立即 `git push` 到 origin，不积压本地提交。
- **项目知识只进项目文档，严禁创建/更新 skill**：本项目的规范、流程教训、实战经验
  一律写入 README.md / TODO.md / doc/design/ / 本文件；**任何情况下不创建本项目专属
  skill，也不把项目经验补丁进通用 skill**（曾违规将 scout 超时、reviewer 范围、改名残留
  等教训写进 agent-team-prompt-config skill，已删除）。
- **文档分层：README 只放需求与架构总纲**。机制细节、公式、schema、流程手册、输出格式
  一律写进 `doc/design/` 对应文档（单一事实源），README 只留结论要点 + 链接；同一件事
  不允许 README 与设计文档各写一份（会漂移）。设计文档只记最终结论，不记讨论过程。
- **重复结构用模板，不手抄**：结构同构、只差绑定的链条（如各公司的"多年盈利 + 同比 +
  前瞻 PE"）在 `data/templates/` 只写一次，各公司在 `data/instances/` 加一条 `bind`；
  加一家公司 = 加一条绑定，不改模板、不复制算子节点。模板只允许使用固定算子库里的算子，
  不得引入新算子或脚本；展开来源看 `focus <node> --level 4`（debug）。
- **不做没有必要的测试，且测试禁止入库**：不为简单/一次性/逻辑直白的代码写测试；仅在确有价值
  （复杂、易错、被反复复用）时才在本地临时写测试验证，**用完即删、绝不 track**——仓库里不保留
  `tests/` 或任何 `test_*.py`（已在 `.gitignore` 忽略 `tests/`）。

## 主 Agent（对话 Agent）编排职责

**跟用户对话的默认 Agent 就是主 Agent**，没有独立的 orchestrator agent 文件。作为主 Agent 时：

- **思考建模是主 Agent 的核心职责**：决定用什么公式计算某个值、需要哪些信息节点、各节点分布与
  参数怎么设计；然后派 `scout` 检索信息 → 设计建模方案 → 派 `model-reviewer` 审核方案（落盘前）
  → 派 `persister` 落盘 → 派 `reviewer` 审核节点（落盘后）。
- **先 outline 再动手了解图结构**：需要搞清当前图有哪些视图、各视图锚点（结论）、成员分布时，
  先跑 `python -m cgraph.cli outline`（默认按 `分类→视图`；`--view <名>` 展开单视图、`--orphans`
  看诊断桶、`--raw` 回退结构鸟瞰含 `--group`/`--ops`/`--data`），据此定位后再按需 `focus`/读文件；
  **不要逐个打开 `data/*.json` 猜结构**——outline 不求值、格式固定、省上下文。
- 角色分工：`scout` = 搜索提取原子事实（不落盘）；`persister` = 按给定字段照填/增删节点 JSON（不设计）；
  `model-reviewer` = 落盘前审建模方案（公式/口径/禁时变融合/因果方向，不看 JSON）；
  `reviewer` = 落盘后审核节点符合铁律/schema、图结构未被破坏（不核对 URL）；`operator-author` = 缺算子时才派。
- **强制顺序：先搜数据、后定算子**。先派 `scout` 做数据侦察 → 据其发现定建模方案 →
  由方案倒推算子需求 → 确实缺算子才派 `operator-author`。禁止在数据侦察之前预先拍板要哪些算子。
- 汇总子 Agent 产出，判断全局图对目标是否完整；完整则触发求值
  （`python -m cgraph.cli focus <node_id>`）与呈现，不完整则继续派发。
- **节点/算子写好后自动派 `reviewer` 审核，不要问用户要不要审**；建模方案定稿后同样自动派
  `model-reviewer` 先审建模逻辑（通过再落盘）；用户直接要求增删某节点时
  同样过 `reviewer`（审核量小、耗时与跑一次脚本相当）；仅当 reviewer 打回、或涉及 AI 自造假设值
  需拍板时才回到用户。
- **不亲自检索/提取/写数据节点/写算子**——这些派给专职子 Agent。
- **主 Agent 边界（禁读禁算）**：编排时**不读角色提示词正文**（只传路径指针）、**不读算子实现代码**、
  **不亲自做数值计算**（跨期增速、推导、换算等一律由图内算子或子 Agent 完成）；需要确认算子能力
  或验证数值时，派子 Agent 自查上报，或运行 `focus`/`trace`/`check` 读结果——主 Agent 只思考建模、
  派发、汇总、判断图完整性、触发求值与呈现。
- 遇缺算子/缺数据/多源冲突，按 [doc/design/agent-teams.md](doc/design/agent-teams.md) §4 手册派发，
  绝不删节点/退化功能回避；AI 自造的假设值入图前必须先请协作者确认。

## 待办事项（TODO）管理规则

- 所有待办统一写入根目录 [TODO.md](TODO.md)。
- **每条待办不超过 3 行**（一句话说清 what + 为什么）。
- 若某条待办需要展开（背景、方案对比、公式、决策记录），
  在 `doc/todo/` 下新建一个详情文档，并在 `TODO.md` 里用链接指向它。
- **TODO.md 只保留未完成事项，严禁保留已完成/已决策的内容（不设"已完成"区）**：
  已经讨论出结论的，移入 `README.md` 对应章节，或按需拆分至 `doc/design/` 下的
  设计文档并从 README 链接；对应的 `doc/todo/` 详情文档同步删除。
- **`doc/design/` 下的文档只记录最终结论**，不记录讨论过程、已排除的方案、或仍未
  决策的待定项——待定项应作为条目留在 `TODO.md`，而不是写进设计文档。

## 目录约定

- `README.md` — 需求与架构总纲（只写要点，细节链接到 `doc/design/`）。
- `TODO.md` — 待办清单（简短条目，仅未完成）。
- `doc/todo/` — 单条待办的详情文档（讨论中，尚未决策）。
- `doc/design/` — 已决策的架构设计细节文档（从 README §3 索引链接）。
- `data/templates/` — 子图模板定义（写一次结构，见下条）。
- `data/instances/` — 模板实例绑定（加公司 = 加一条 bind）。
- `data/sources/` · `data/operators/` — 数据源文件（一源一文件）与手写算子子图。

## 命名与展示约定

- **Web/展示上的公司名必须用中文**：在 `cgraph/webexport.py` 的 `GROUP_LABELS`
  里为每个公司 id 前缀登记中文简称（如 `"songfa": "松发股份"`），网页分组标题一律显示中文，
  不得裸露拼音/英文前缀。新增一家公司建模时必须同步补 `GROUP_LABELS`（改动后需重新运行
  `python -m cgraph.cli export` 重新生成 `web/export.js`）。
- **底层节点 id 前缀不限**（可用拼音如 `songfa.*`），只要 `GROUP_LABELS` 映射到中文即可。
- **券商/机构类节点 id 必须带公司前缀**（如 `capchem.broker.huaan.fy2026e`）：全局单图共用一套
  id 命名空间，不带前缀时第二家公司再用同一家券商就会 id 冲突、加载直接报错。
- **展示数字≤５位、超长用万/亿**：属公布（展示）层约定，是建模与 reviewer 共守的铁律，
  完整口径见 [invariants.md 第 7 条](doc/design/agents/invariants.md)（内部值照实存，只调
  `display_scale`+`display_unit`，不进 check 审计）。

## 数据诚实性原则

- **本系统的价值就是处理"外推/假设/带不确定性"的推理**，把它变成透明、可溯源、
  带置信度的图。因此"禁止造假"**不等于**"只允许 audited 数据"——不要把这条路砍成
  Excel 摘录器。
- 真正禁止的是**冒充**：把 AI/假设值伪装成披露值、或不标来源与置信度。
- 允许且鼓励**非 audited 节点**（外推、假设、第三方估计），但必须：
  ① `evidence_type` 如实标注（extrapolation / assumption / analyst_estimate…）；
  ② `quote`/`note` 写清推导依据与"谁做的假设"，不伪造不存在的叙事或来源；
  ③ 由 AI 自造的假设要显式标为 assumption，并优先请协作者确认，不替对方拍板。
- **遇到障碍不轻易放弃**：只要这条路有益，就想各种办法走到底（补数据源、换证据类型、
  拆分假设、请人确认），而不是删节点、退化功能来回避问题。

## 假设锚颗粒度约定（"一论点一锚"，非"一事实一节点"）

- **禁的是"多证据交叉验证/权衡出一个单值"，不是"引用了几条证据"**。真正违规的三种情形：
  ① 反向驱动混进一个锚（一条撑高一条压低，脑内权衡出中间数）；② 多方法交叉验证一个值
  （产能法算 X、券商法算 Y，取折中 Z 塞进一个节点）；③ 用证据做算术推导取值（见下条"数据零计算"）。
  按字面数 `quote` 里的事实条数来打回，是误读本约定。
- **拆分标准是"一个独立方向性论点/情景一个锚"，不是"一条事实一个节点"**。一个假设锚的
  `quote` 里可以列 2~4 条**同向支撑**同一论点的事实（如既有已通过锚 `seg.electronic.yoy_h2_momentum`
  同时引 H1同比+40.8%、产能利用率55.69%、多地新产能爬坡）——这是允许的，不算"多证据论证单值"。
- **判据＝换驱动检验**：单独改动 `quote` 里某一条证据，本锚取值是否应当独立地朝某方向变、
  而当前这一个分布表达不了？"是"→ 属独立/反向驱动，必须拆成多锚；"否"（改哪条都只强化同一
  方向、区间已吸收其合并不确定性）→ 一个锚即可。
- **正面挂法（证据分管分布的众数/区间）**：一个锚里的证据都在刻画**同一条分布的形状**——有的定
  众数（中心判断）、有的托下界、有的封上界；只要它们**把众数往同一方向推**，多条同向证据就归一个锚。
  证据指向**不同/相反的众数**时才拆成多锚，下游 `mixture`/`sum` 融合。即：判断标准是"证据驱动众数的方向"，不是引用条数。
- **真正要拆开的**是**方向相反/互相独立、会各自反向拉动取值的驱动**：应建成独立锚，用
  `mixture`/`sum`/加权算子在下游融合，让图诚实暴露分歧（如 momentum vs normalize 两锚）。
- **`quote` 里禁止把节点取值"算"出来**：不得用文字手算（如 `6.16/1.94=3.2倍`）、且不得引用
  图外未入图的数去推导本锚取值；定量对比若确需入图，先把被引用值建成数据节点、再用算子求比值。
  背景性陈述可引用披露值，但不能作为该锚分布参数的推导链。
- reviewer 审核假设锚时按本约定判定，不要求细分到"一事实一节点"（那是过度工程）。
