# AGENTS.md — CausalGraph 协作约定

面向 AI Agent 与协作者的项目约定。开始工作前先读本文件。

## 协作行为约定

- **"记住" = 写进本文件**：只要协作者说"记住 X / 记一下 X"，就把 X 追加进
  本 `AGENTS.md`（而不是别处的 memory）。
- **不做没有必要的测试**：不为简单/一次性/逻辑直白的代码写测试；仅在确有价值
  （复杂、易错、被反复复用）时才加。

## 主 Agent（对话 Agent）编排职责

**跟用户对话的默认 Agent 就是主 Agent**，没有独立的 orchestrator agent 文件。作为主 Agent 时：

- 把目标拆成可执行的**数据/算子子任务**并分发：数据/来源提取 → `scout`；缺算子 → `operator-author`；
  新建产物 → `reviewer` 审核（三者为 `.github/agents/*.agent.md` 子 Agent）。
- **强制顺序：先搜数据、后定算子**。先派 `scout` 做数据侦察 → 据其发现定建模方案 →
  由方案倒推算子需求 → 确实缺算子才派 `operator-author`。禁止在数据侦察之前预先拍板要哪些算子。
- 汇总子 Agent 产出，判断全局图对目标是否完整；完整则触发求值
  （`python -m cgraph.cli focus <node_id>`）与呈现，不完整则继续派发。
- **节点/算子写好后自动派 `reviewer` 审核，不要问用户要不要审**；仅当 reviewer 打回、
  或涉及 AI 自造假设值需拍板时才回到用户。
- **不亲自检索/提取/写数据节点/写算子**——这些派给专职子 Agent。
- 遇缺算子/缺数据/多源冲突，按 [doc/design/agent-teams.md](doc/design/agent-teams.md) §4 手册派发，
  绝不删节点/退化功能回避；AI 自造的假设值入图前必须先请协作者确认。

## 待办事项（TODO）管理规则

- 所有待办统一写入根目录 [TODO.md](TODO.md)。
- **每条待办不超过 3 行**（一句话说清 what + 为什么）。
- 若某条待办需要展开（背景、方案对比、公式、决策记录），
  在 `doc/todo/` 下新建一个详情文档，并在 `TODO.md` 里用链接指向它。
- **TODO.md 只保留未完成事项，严禁保留已完成/已决策的内容（不设"已完成"区）**：
  已经讨论出结论的，移入 `README.md` 对应章节，或按需拆分至 `doc/design/` 下的
  设计文档并从 README 链接；对应的 `doc/todo/` 详情文档同步删除。
- **`doc/design/` 下的文档只记录最终结论**，不记录讨论过程、已排除的方案、或仍未
  决策的待定项——待定项应作为条目留在 `TODO.md`，而不是写进设计文档。

## 目录约定

- `README.md` — 需求与架构总纲。
- `TODO.md` — 待办清单（简短条目，仅未完成）。
- `doc/todo/` — 单条待办的详情文档（讨论中，尚未决策）。
- `doc/design/` — 已决策的架构设计细节文档（从 README 链接）。

## 数据诚实性原则

- **本系统的价值就是处理"外推/假设/带不确定性"的推理**，把它变成透明、可溯源、
  带置信度的图。因此"禁止造假"**不等于**"只允许 audited 数据"——不要把这条路砍成
  Excel 摘录器。
- 真正禁止的是**冒充**：把 AI/假设值伪装成披露值、或不标来源与置信度。
- 允许且鼓励**非 audited 节点**（外推、假设、第三方估计），但必须：
  ① `evidence_type` 如实标注（extrapolation / assumption / analyst_estimate…）；
  ② `quote`/`note` 写清推导依据与"谁做的假设"，不伪造不存在的叙事或来源；
  ③ 由 AI 自造的假设要显式标为 assumption，并优先请协作者确认，不替对方拍板。
- **遇到障碍不轻易放弃**：只要这条路有益，就想各种办法走到底（补数据源、换证据类型、
  拆分假设、请人确认），而不是删节点、退化功能来回避问题。
